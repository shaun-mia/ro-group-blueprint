import { Layout } from '@/components/layout'
import { ventures } from '@/data/ventures'
import Link from 'next/link'

export default function CareersPage() {
  return (
    <Layout>
      <div className="container mx-auto px-4 py-8">
        <h1 className="text-4xl font-bold mb-8">Careers at RO Group</h1>
        <h2 className="text-2xl mb-4">Empowering Your Growth: Build Your Future With Us</h2>
        
        <section className="mb-12">
          <h3 className="text-2xl font-bold mb-4">A Culture of Opportunity and Innovation</h3>
          <p className="mb-4">At RO Group, our motto "Empowering Growth Every Step" applies as much to our employees as it does to our businesses.</p>
          
          <div className="grid grid-cols-1 md:grid-cols-2 gap-6 mt-6">
            {/* Benefits cards */}
          </div>
        </section>

        <section className="mb-12">
          <h3 className="text-2xl font-bold mb-4">Find Your Fit</h3>
          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
            {ventures.map((venture) => (
              <Link key={venture.slug} href={`/careers/${venture.slug}`}>
                <div className="border p-6 rounded-lg hover:shadow-lg transition-shadow">
                  <h4 className="font-bold">Join the {venture.name} Team</h4>
                </div>
              </Link>
            ))}
          </div>
        </section>
      </div>
    </Layout>
  )
}